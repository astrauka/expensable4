h1
  ' new group
  = link_to "back",
            [:user, :groups],
            class: "btn btn-default btn-sm"

= simple_form_for group,
                  url: [:user, group],
                  html: { class: "form-vertical" } do |f|
  = render "fields", f: f
  = f.button :submit,
             "submit",
             data: { action: 'submit' },
             class: "btn btn-primary"
